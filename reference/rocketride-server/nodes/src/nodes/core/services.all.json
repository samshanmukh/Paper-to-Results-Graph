{
	//
	// Handle the all. - combines services into a single selectable type
	//
	"description": ["A combined configuration that bundles a preprocessor, embedding model, vector store, and LLM into a single selectable unit."],
	"fields": {
		"all.preprocessor": {
			"title": "Preprocessor",
			"default": "preprocessor_langchain",
			"combo": "preprocessor"
		},
		"all.embedding": {
			"title": "Embedding",
			"default": "embedding_transformer",
			"combo": "embedding"
		},
		"all.store": {
			"title": "Vector Store",
			"default": "qdrant",
			"combo": "store"
		},
		"all.llm": {
			"title": "LLM",
			"default": "openai",
			"combo": "llm"
		}
	}
}
