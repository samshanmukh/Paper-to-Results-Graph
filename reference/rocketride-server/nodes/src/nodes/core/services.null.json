{
	//
	// Required:
	//		The displayable name of this node
	//
	"title": "Local File System",
	//
	// Required:
	//		The protocol is the endpoint protocol
	"protocol": "null://",
	//
	// Required:
	//		Class type of the node - what it does
	//
	"classType": [],
	//
	// Required:
	//		Capabilities are flags that change the behavior of the underlying
	//		engine
	"capabilities": ["internal"],
	//
	// Optional:
	//		Description to of this driver
	//
	"description": ["An internal null endpoint that serves as a no-op source and target, forwarding content into the pipeline without reading from or writing to an external system."],
	//
	// Optional:
	//		Supported actions - if not specified, no actions are supported
	//		engine
	//
	"actions": [],
	//
	// Required:
	//		The prefix map when added/removed when convertting URLs <=> paths
	//
	"prefix": "Null",
	//
	//	Optional:
	//		As a pipe component, define what this pipe component takes
	//		and what it produces
	//
	"lanes": {
		"source": ["tags"]
	},
	//
	// Optional:
	//		Local fields definitions - these define fields only for the
	//		current service. You may specify them here, or directly
	//		in the shape
	"fields": {
		"null.source.parameters": {
			"section": "parameters",
			"title": "Parameters",
			"properties": []
		},
		"null.target.parameters": {
			"object": "parameters",
			"title": "Parameters",
			"properties": []
		}
	},
	//
	// Required:
	//		Defines the fields (shape) of the service. Either source or target
	//		map be specified, or both, but at least one is required
	"shape": [
		{
			"section": "Source",
			"title": "Null Source",
			"properties": ["type", "actions", "source.mode", "null.source.parameters"]
		},
		{
			"section": "Target",
			"title": "Null Target",
			"properties": ["type", "target.mode", "null.target.parameters"]
		}
	]
}
