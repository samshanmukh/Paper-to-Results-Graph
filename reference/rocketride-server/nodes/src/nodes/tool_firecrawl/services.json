{
	"title": "Firecrawl",
	"protocol": "tool_firecrawl://",
	"classType": ["tool"],
	"capabilities": ["invoke"],
	"register": "filter",
	"node": "python",
	"path": "nodes.tool_firecrawl",
	"prefix": "firecrawl",
	"icon": "firecrawl.svg",
	"documentation": "https://docs.rocketride.org",
	"description": ["Exposes Firecrawl web-scraping operations as agent tools.", "Provides scrape_url (single page) and map_url (site structure discovery)."],
	"tile": [],
	"lanes": {},
	"preconfig": {
		"default": "default",
		"profiles": {
			"default": {
				"title": "Firecrawl",
				"apikey": ""
			}
		}
	},
	"fields": {
		"tool_firecrawl.apikey": {
			"type": "string",
			"title": "API Key",
			"description": "Firecrawl API key",
			"default": "",
			"secure": true,
			"ui": {
				"ui:widget": "ApiKeyWidget"
			}
		}
	},
	"shape": [
		{
			"section": "Pipe",
			"title": "Firecrawl",
			"properties": ["type", "tool_firecrawl.apikey"]
		}
	]
}
