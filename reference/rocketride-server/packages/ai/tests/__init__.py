# Unit tests for the ai package.
#
# Layout mirrors the package under test: tests/ai/common/models/audio/test_whisper.py
# tests ai.common.models.audio.whisper. Add further tests under tests/ai/... as needed.
#
# Run from project root (PYTHONPATH must include packages/ai/src):
#   PYTHONPATH=packages/ai/src python -m pytest packages/ai/tests -v
# Or from dist/server (ai already on path):
#   engine -m pytest ../../packages/ai/tests -v
